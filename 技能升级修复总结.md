# 技能升级修复总结

## 修复的问题

### 1. 后端技能升级逻辑错误
**问题**: 在 `SkillService.upgradeSkill` 方法中，第 278 行有一个逻辑错误：
```java
playerSkill.setExperience(playerSkill.getExperience() + 10);
```
这行代码在扣除升级所需经验后，又给技能增加了 10 点经验，导致技能经验计算错误。

**修复**: 移除这行多余的代码，确保升级后技能经验正确扣除。

### 2. 前端缺少技能升级API方法
**问题**: 前端 `api.js` 中只有 `upgradeSkillByPoints` 方法（使用技能点升级），缺少 `upgradeSkill` 方法（使用灵石升级）。

**修复**: 在 `api.js` 中添加 `upgradeSkill` 方法：
```javascript
async upgradeSkill(playerSkillId) {
    return await api.post(`/skills/${playerSkillId}/upgrade`);
}
```

## 修复的文件

### 1. SkillService.java
位置: `src/main/java/com/xiuxian/game/service/SkillService.java`
- 修复 `upgradeSkill` 方法中的经验计算错误

### 2. api.js
位置: `src/main/resources/static/js/api.js`
- 添加 `upgradeSkill` 方法（使用灵石升级）

### 3. 测试脚本
- `test-skill-upgrade.sh`: 测试技能升级功能的脚本

## 技能升级功能说明

### 升级方式
1. **灵石升级**: 消耗灵石升级技能
2. **技能点升级**: 消耗技能点升级技能

### 升级消耗计算
```java
// 升级所需经验计算
int requiredExp = 100 + (currentLevel - 1) * 50;
```

### 升级流程
1. 检查技能是否存在
2. 检查是否已达到最大等级
3. 检查经验/技能点是否足够
4. 扣除经验/技能点
5. 提升技能等级
6. 更新数据库

## API 接口

### 升级技能（使用灵石）
```
POST /api/skills/{playerSkillId}/upgrade
```

### 升级技能（使用技能点）
```
POST /api/skills/{playerSkillId}/upgrade-by-points
```

## 测试方法

### 1. 使用测试脚本
```bash
chmod +x test-skill-upgrade.sh
./test-skill-upgrade.sh
```

### 2. 手动测试
1. 登录游戏
2. 学习一个技能
3. 使用灵石升级技能
4. 使用技能点升级技能
5. 查看技能等级和经验是否正确更新

## 总结

技能升级的所有已知bug已修复，包括：
- 后端技能升级经验计算错误
- 前端缺少技能升级API方法

现在技能升级功能可以正常工作。