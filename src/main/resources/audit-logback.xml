# 安全审计日志配置

logging:
  level:
    com.xiuxian.game.common.audit: INFO
  
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    
  # 审计日志独立文件
  file:
    name: logs/audit.log
    max-size: 100MB
    max-history: 30
    total-size-cap: 3GB
