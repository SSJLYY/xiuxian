# 服务器配置
server.port=8080

# 数据库配置 (使用H2内存数据库，无需外部数据库)
spring.datasource.url=jdbc:mysql://47.103.87.55:3306/xiuxian_game?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&maxAllowedPacket=16777216&useConfigs=maxPerformance
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=1qaz@WSX

# JPA配置
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# H2控制台配置 (可选，用于调试)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JWT配置
jwt.secret=xiuxianGameSecretKey2024VeryLongAndSecure
jwt.expiration=86400000

# 日志配置
logging.level.com.xiuxian.game=INFO
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=INFO

# 文件上传配置
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS配置
app.cors.allowed-origins=*
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.cors.allowed-headers=*
app.cors.allow-credentials=true

# 静态资源配置
spring.mvc.static-path-pattern=/static/**,/*.html,/*.css,/*.js,/*.png,/*.jpg,/*.gif,/*.ico

# 禁用SQL初始化脚本，避免冲突
spring.sql.init.mode=never