# 2026-04-07 工作日志

## 第二轮全量代码审计与修复（再来一轮）

### 修复清单（10个文件，全量编译 BUILD SUCCESS）

#### AuctionService.java（5处）
- `Integer != itemId` → `Objects.equals()` 修复3处（ITEM/EQUIPMENT/PET类型判断）
- `cancelAuction` 事务顺序修正：先 `cancelAuctionItem` 原子SQL，再扣取消手续费
- `processExpiredAuctions` 提取 `processOneExpiredAuction` 独立事务方法，per-item 隔离
- 死代码清除：`addItemToBuyerInventory` 和 `addItemToSellerInventory` 中无用的 `newEquipment`/`newPet` 对象
- 补充 `@Slf4j` 注解（之前用的是 MyBatis `Log`，不支持多参数 error）

#### SecurityFilter.java
- 移除 `shouldSkipFilter` 中 `/api/auth/` 的豁免，黑名单 IP 现在也受登录/注册接口过滤

#### RedisConfig.java（3处）
- `LaissezFaireSubTypeValidator` → `BasicPolymorphicTypeValidator`（限制反序列化包路径）
- 提取公共 `buildJsonSerializer()` 方法，消除两处重复 ObjectMapper 构建
- `tokenCache` TTL 从 24h 对齐到 JWT accessToken 的 2h

#### LoggingAspect.java（3处）
- 切点表达式修正：`com.xiuxian.game.controller..*` → `com.xiuxian.game.modules.*.controller..*`（同理 service）
- 重复异常日志消除：Controller/Service 异常处理中去掉 `log.error`，只保留 `LogUtils.logError`

#### PlayerService.java（2处）
- Magic Number 提取为10个命名常量（BREAKTHROUGH_COST/SUCCESS_RATE/MAX_LEVEL等）
- 补回因替换操作意外删除的 `playerProfileMapper` 字段声明

#### SecurityConfig.java（2处）
- 移除 `/api/auth/validate` 的 `permitAll`（需要有效 Token）
- `/**/*.html` 通配符精确化为 `/*.html`（单层匹配）
- `List.of("*")` → `Arrays.asList("*")`（Java 8 兼容）

#### DataSourceAspect.java
- 移除永远不会执行的 `annotation == null` 死代码分支，替换为防御性兜底注释

#### RateLimitAspect.java
- 为永远不执行的 `rateLimit == null` 分支添加防御性兜底注释

#### 预存编译错误修复（顺手修复）
- `AntiFraudService.java`：补充缺失的 `@Scheduled` import
- `RechargeService.java`：补充缺失的 `@Slf4j` 注解
- `GuildBossController.java`：`vo.getBossId()` → `vo.getId()`（字段名不符）
- `AdminController.java`：`item.getName()` → `item.getShopType()`（ShopItem 无 name 字段）

### 安全级别统计
- 🔴 已修复 2（IP黑名单绕过、LaissezFaireSubTypeValidator RCE风险）
- 🟠 已修复 3（Integer != Bug、cancelAuction事务、processExpiredAuctions事务）
- 🟡 已修复 5（TTL对齐、重复日志、切点路径、死代码等）
